// just combines abi and contractAddresses in single object

const contractAddresses = require("./contractAddresses.json");
const abi = require("./abi.json");

module.exports = { contractAddresses, abi };
